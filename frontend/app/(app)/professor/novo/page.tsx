// Criar novo professor — placeholder.
// Implementação completa virá no item 8 do roadmap (formulário + POST /professores).
export default function NovoProfessorPage() {
  return (
    <div className="space-y-4">
      <h2 className="text-2xl font-bold">Novo professor</h2>
      <p className="text-muted-foreground">Formulário será implementado em seguida.</p>
    </div>
  );
}
