// Tela de login — placeholder. Implementação real (Supabase Auth) virá depois.
export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <div className="w-full max-w-md space-y-4 rounded-lg border p-8">
        <h1 className="text-2xl font-bold">ProfessorIA</h1>
        <p className="text-muted-foreground">
          Tela de login (placeholder). Será implementada com Supabase Auth.
        </p>
      </div>
    </main>
  );
}
